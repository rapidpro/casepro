from __future__ import unicode_literals

from datetime import timedelta

# import our default settings
from settings_common import *

# INSTALLED_APPS = INSTALLED_APPS + ('debug_toolbar.apps.DebugToolbarConfig',)
# MIDDLEWARE_CLASSES += ('debug_toolbar.middleware.DebugToolbarMiddleware',)

DEBUG = True
TEMPLATE_DEBUG = DEBUG

HOSTNAME = 'localhost:8000'

SITE_API_HOST = 'http://localhost:8001/'

UNLABELLED_LIMIT_DAYS = 40

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'casepro',
        'USER': 'casepro',
        'PASSWORD': 'nyaruka',
        'HOST': 'localhost',
        'PORT': '',
        'ATOMIC_REQUESTS': True,
        'OPTIONS': {
        }
    }
}

# speed up the background tasks
CELERYBEAT_SCHEDULE['message-pull']['schedule'] = crontab(minute='*/2')         # every even minute
CELERYBEAT_SCHEDULE['contact-pull']['schedule'] = crontab(minute='1-59/2')      # every odd minute
CELERYBEAT_SCHEDULE['delete-old-incoming']['schedule'] = crontab(minute='*/5')  # every 5 minutes
